---
galaxy_info:
  role_name: loadbalancer
  namespace: platform
  author: Platform Engineering
  description: HAProxy + keepalived edge load balancer pair (L7 TLS termination or L4 PROXY-protocol passthrough) in front of Kong / Istio, Kubernetes API and Kafka
  license: Apache-2.0
  min_ansible_version: "2.16"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
  galaxy_tags: [haproxy, keepalived, loadbalancer, vrrp]
dependencies: []
